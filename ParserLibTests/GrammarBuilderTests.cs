using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ParserLib
{
   [TestClass]
   public class GrammarBuilderTests
   {
      [TestMethod]
      public void Test()
      {
         //var grammar = new Grammar();
         //var builder = new GrammarBuilder<ExpressionType>(grammar);
         //builder.AddToken(ExpressionType.openClamp)
         //       .AddBranch()
         //         .AddToken(ExpressionType.diggit)
         //       .Next()
         //         .AddToken(ExpressionType.openClamp)
         //         .AddToken(ExpressionType.diggit)
         //         .AddToken(ExpressionType.closeClamp)
         //       .CloseBranch();
      }
   }
}
